# 网站变更监测功能实施方案与实现报告

## 1. 目标与范围

在现有管理后台增加仅管理员可见的“网站监测”Tab，由阿里云服务器上的后端独立轮询目标站点，并复用 GitHub 日报的 SMTP 通道向 `1296757861@qq.com` 发送变更通知。浏览器关闭后监测仍可运行，首次成功读取只建立基线，不发送历史内容。

| 监测源 | 固定地址 | 判断内容 | 固定周期 |
| --- | --- | --- | --- |
| JustWoker 模型广场 | `https://api.justwoker.icu/api/pricing` | 模型新增、模型下架 | 30 分钟（1800 秒） |
| 惠州学院研究生招生 | `https://www.hzu.edu.cn/yjszs/list.htm` | 新研究生招生公告 | 60 分钟（3600 秒） |

JustWoker 的模型数据来自需认证的 `GET /api/pricing` JSON 接口；`/pricing` 只是单页应用文档，不能作为模型数据源。生产定时监测采用正常登录产生的刷新会话 Cookie：后端先调用 `POST /api/user/auth/refresh` 换取本次运行使用的短期 Bearer Token，再访问模型接口；临时 Bearer 仅保留用于短时诊断。该方案不模拟账号密码登录、不绕过 Turnstile 或其他登录控制。惠州学院列表页可以直接获取。认证信息使用项目现有 `encrypt()`/`decrypt()` 兼容机制处理，管理员接口只返回是否配置及掩码，不返回明文。第一至第六阶段分别给出了数据库、解析、抓取、邮件投递、定时心跳、管理员 API 与管理员界面的实现和分层核验；第七阶段执行跨层回归、真实站点核验、发布包审计、可回滚上线和生产验收。生产环境保持 JustWoker 监测开关关闭：惠州学院已经建立 14 条公告基线，JustWoker 等待管理员配置有效刷新会话 Cookie 后再建立基线；凭据失效会有邮件告警（见 9.9），不再静默停摆。

## 2. 总体技术路线

```text
管理员 Tab → 管理员 API → SQLite 配置/状态
                              ↓
cron 心跳 → 到期查询 → 安全抓取 → 防御性解析 → 快照差异
                                             ↓
                                      幂等事件 → 邮件队列 → SMTP
```

调度采用短周期心跳查询 `next_run_at`，实际业务间隔由数据库约束固定为 30/60 分钟。抓取器只接受预设 HTTPS 主机；解析器只有在响应完整、结构合法且结果非空时才输出快照；事务层将项目、事件、运行结果和下次执行时间作为一致数据提交。这样可以把网络失败、认证失效、页面改版与真实下架区分开。

## 3. 第一阶段：数据库持久化的实现方式与核验

### 3.1 表结构与约束

`lcyksp-backend/src/config/db.js` 建立以下五张表：

- `site_monitors`：保存两个来源的固定 URL、周期、开关、密文凭据、HTTP 验证器、健康状态和下次执行时间；
- `site_monitor_items`：保存模型或公告的稳定键、展示字段、活跃状态和连续缺失次数；
- `site_monitor_runs`：保存每次计划、手动、诊断或基线运行的状态、耗时和计数；
- `site_monitor_events`：保存模型新增、模型下架和公告新增事件，`event_key` 唯一约束负责持久化去重；
- `site_monitor_deliveries`：为后续可靠邮件投递保存收件人、正文、重试次数和投递状态。

外键均指向监测配置并按用途设置级联行为；布尔值、枚举、非负计数都有 `CHECK` 约束；`UNIQUE(monitor_id, item_key)` 防止同一项目重复。到期任务、活跃项目、最近运行、最近事件和待投递邮件各有索引。数据库强制 JustWoker 为 1800 秒、惠州学院为 3600 秒，避免前端或错误 SQL 把轮询改成高频请求。

### 3.2 初始化与测试隔离

两条默认配置使用 `ON CONFLICT(source) DO NOTHING` 幂等插入，默认关闭，因此 PM2 重启不会覆盖管理员配置，也不会在 SMTP 和凭据尚未诊断时自动访问。`LCYKSP_DB_DIR` 允许测试使用临时数据库，`closeDb()` 负责释放句柄，测试不接触部署数据库。

### 3.3 审计与验证

使用全新临时库和重复初始化核验了表、索引、两条默认记录及幂等行为；非法周期会被 SQLite 约束拒绝；外键检查无异常，完整性检查返回 `ok`。`node --check` 验证数据库模块语法。该阶段为事件去重、失败追踪、二次下架确认和邮件重试提供了可恢复的持久状态。

## 4. 第二阶段：解析、规范化与差异引擎的实现方式与核验

### 4.1 JustWoker 模型解析

`lcyksp-backend/src/utils/siteMonitorParsers.js` 只接受经过白名单定义的 JSON 容器，并从稳定字符串 ID 或名称生成规范化键。空响应、HTML 登录页、非法 JSON、`success: false`、未知结构、空模型集合和非法条目均抛出错误，不会把失败响应解释成“全部模型下架”。解析限制为 10,000 个模型，键最长 512 字符、标题最长 1,000 字符，阻止异常上游内容造成无界内存和数据库膨胀。

### 4.2 惠州学院公告解析

公告解析只接收惠州学院同源并符合文章路径规则的链接，提取文章 ID、标题、日期和规范 URL。实现包含中文及数字实体解码、嵌套标签文本提取、标题属性回退、查询参数移除和文章 ID 去重。HTML 上限为 5 MB，公告上限为 5,000 条；空白页和结构无关页面直接失败，因此站点改版会保留旧基线而不是误报。

### 4.3 差异与事件标识

集合差异按稳定键判断并忽略顺序及重复项。当前快照为空会被拒绝。事件指纹对 `[source, eventType, itemKey]` 的 JSON 序列化结果执行 SHA-256，避免简单分隔符拼接在字段含特殊字符时产生歧义，同时使重试获得同一事件键。

### 4.4 审计与验证

脱敏夹具覆盖多种 JSON 结构、模型新增/下架、顺序变化、重复项、非法及超限输入、公告置顶、实体解码和指纹边界。惠州学院真实页面交叉验证得到 HTTP 200、34,408 字节和 14 条唯一公告，标题、日期及同源 URL 均符合预期。JustWoker 初始核验阶段没有可用管理员凭据，因此先用脱敏夹具和模拟响应验证，未尝试绕过认证；后续根据浏览器 XHR 和生产服务器使用加密凭据的只读探测，确认真实接口为 `GET /api/pricing`，返回 `application/json`，有效数据位于 `data` 数组并以 `model_name` 标识模型。

## 5. 第三阶段：安全抓取器与监测服务的实现方式与核验

### 5.1 抓取安全边界

`lcyksp-backend/src/utils/siteMonitorFetch.js` 使用 `undici`，配置 10 秒连接超时和 20 秒总超时。URL 必须使用 HTTPS，主机必须精确等于 `api.justwoker.icu` 或 `www.hzu.edu.cn`，并拒绝用户名、密码、自定义端口及伪造子域名。每次请求和每一跳重定向都会重新做 URL 与 DNS 检查；私网、回环、链路本地等地址被拦截，最多允许 3 次重定向，以降低 SSRF 与 DNS rebinding 风险。

JustWoker 响应上限 2 MB，惠州学院上限 5 MB，同时校验 `Content-Length` 和实际流读取量。响应必须具有来源允许的 `Content-Type`，401/403、异常 HTTP 状态、HTML 登录页、空正文及超大正文都作为失败。Cookie/Bearer 值拒绝 CRLF，返回值和错误摘要不包含认证明文。正常轮询支持 `ETag`、`Last-Modified` 和 304，减少服务器带宽压力；诊断模式主动移除条件请求头，以保证获取可解析正文且不修改原验证器。

### 5.2 基线、差异和误报控制

`lcyksp-backend/src/utils/siteMonitorService.js` 从 SQLite 加载配置并只在服务端解密凭据。首次完整成功快照写入基线但不生成历史事件；后续模型新增立即生成 `model_added`；模型必须在连续两次成功快照中缺失才置为下架并生成 `model_removed`；公告出现新文章 ID 时生成 `announcement_added`。任何抓取或解析失败都不修改基线。

事件插入使用唯一指纹幂等处理。成功运行记录项目数和变更数，失败运行记录脱敏错误、连续失败次数及下一次退避时间。退避从 5 分钟指数增加，但不超过该来源正常周期。诊断运行仅写自己的运行记录，不改基线、监测健康状态或调度时间。

### 5.3 并发和事务审计修正

项目使用单个 `sqlite3` 连接。若两个来源并行进入 `BEGIN IMMEDIATE`，会产生嵌套事务风险，因此监测入口加入进程级串行队列；同一来源仍通过 in-flight Promise 复用，避免手动触发和调度触发重复执行。串行化范围覆盖完整运行，保证其他来源的 SQL 不会插入当前事务。

成功路径把项目 upsert、缺失次数、事件插入、运行结束状态、HTTP 验证器、健康状态和下次执行时间放入同一个事务。任一写入失败会整体回滚，不会留下“事件已写入但运行仍显示 running”的部分状态。304 与失败状态分别使用独立原子事务。进程异常终止仍可能留下历史 `running` 记录，后续 API/启动恢复阶段需把超时记录标记为中断；这不影响基线和事件的一致性。

### 5.4 验证结果

Node 测试覆盖 URL 白名单、DNS 阻断、重定向上限、认证头、条件请求、304、响应类型与大小、登录页、空正文、首轮基线、单次通知、二次下架、失败不覆盖基线、公告重排、诊断隔离、诊断强制正文和双来源并发。相关抓取、解析与服务测试纳入统一后端测试集，最终 62/62 通过；相关 JavaScript 文件通过 `node --check`。惠州学院真实抓取 smoke test通过；JustWoker 的真实认证链路由管理员保存 Bearer Token 后通过诊断接口核验。

## 6. 第四阶段：邮件复用与可靠投递的实现方式与核验

### 6.1 SMTP 通道复用

`lcyksp-backend/src/utils/githubMail.js` 的内部发送函数增加 `fromName` 参数，并导出 `sendSiteMonitorEmail()`，发件人显示名为“lcyksp.xyz 网站监测”。连接方式、465/STARTTLS 分支、AUTH LOGIN、收件地址规范化、CRLF 清除和日志脱敏全部沿用日报已在生产运行的同一段实现，没有引入第二套 SMTP 代码，也没有新增依赖。

### 6.2 邮件内容生成

`lcyksp-backend/src/utils/siteMonitorMailer.js` 的 `renderSiteMonitorEmail()` 按新增模型、下架模型、新公告分组，主题形如 `【网站监测】JustWoker 模型广场：新增 2 个模型、下架 1 个模型`。所有标题、日期、来源名都做 HTML 转义；只有 `https://` 链接才渲染为 `<a>`，`javascript:` 等一律降级为纯文本；单封最多列出 100 条，超出部分显示“另有 N 项未在邮件中列出”，主题截断 200 字符并清除 CR/LF，避免异常上游内容造成头注入或超大正文。

### 6.3 队列写入与幂等

事件插入函数改为返回“本次是否真的新建”（依据 `ON CONFLICT(event_key) DO NOTHING` 的影响行数），只有新事件进入邮件批次，已通知过的事件不会因重跑再次发信。投递去重键对 `[source, 去重排序后的 event_key 列表]` 的 JSON 序列化结果执行 SHA-256，配合 `ON CONFLICT(dedupe_key) DO NOTHING`，同一批变更无论重试多少次都只有一封邮件。投递行与项目快照、事件、运行结果、HTTP 验证器和下次执行时间在同一个事务提交；首轮基线和诊断运行不入队。

### 6.4 投递、重试与中断恢复

`processSiteMonitorDeliveries()` 先回收僵死记录，再取出到期 `pending`，单次最多 5 封。投递前用去重键做条件更新锁定批次（`status='pending'` → `'sending'` 且 `attempt_count + 1`，影响行数必须为 1），因此重叠心跳或管理员手动触发都不会重复发信。成功写入 `sent_at` 并清空错误；失败按 5/10/20/40 分钟指数退避（上限 60 分钟），累计 5 次投递失败后置为 `failed`，错误摘要脱敏并截断 500 字符。`sending` 超过 10 分钟视为进程中断：未达重试上限的重新排队，已达上限的直接置 `failed`，不会无限重试也不会永久卡住。进程内同一次投递过程通过 in-flight Promise 复用。

### 6.5 单连接事务隔离修正

第三阶段的进程级串行队列提取为 `lcyksp-backend/src/utils/siteMonitorQueue.js` 共享模块。监测运行与投递的语句组都经过该队列，避免投递的 `UPDATE` 落进监测运行已打开的事务里，被该事务回滚后造成“邮件已发出但状态回退成 pending”的重复发送。SMTP 会话留在队列之外，最长 30 秒的连接不会阻塞监测运行。

### 6.6 验证结果

新增 14 条测试，站点监测累计 43/43 通过；相关 JavaScript 文件通过 `node --check`。覆盖渲染分组与转义、链接白名单、指纹忽略顺序与重复、基线不入队、变更入队一封、重复运行不再入队、重复入队为空操作、成功后写入 `sent_at` 且不再重发、失败保留批次并退避、达到上限置 `failed`、已被锁定的批次不被他人取走、中断恢复的两种走向、重叠心跳只发一次、与监测运行并发时两封各发一次、未配置 SMTP 时批次保留、管理员测试邮件直发不入队。

本阶段用注入式传输替代本地 TCP 模拟 SMTP：生产发送函数强制 TLS/STARTTLS，回环模拟需要关闭证书校验，代价高于收益；SMTP 报文构造仍由已在生产运行的日报通道负责，本阶段验证的是收件人、主题、正文、幂等、锁定与重试。其中“未配置 SMTP”一例调用真实发送函数，确认它在建立连接之前就失败并让批次保留待重试。测试过程未发送任何真实邮件。

心跳接线属于第五阶段，因此当前入队的邮件要等第五阶段接入 `cron.js` 后才会自动发出。

## 7. 第五阶段：调度与管理员 API 的实现方式与核验

### 7.1 心跳接线与启动恢复

`lcyksp-backend/src/utils/cron.js` 的 5 分钟心跳里增加站点监测：先 `runDueSiteMonitors()` 再 `processSiteMonitorDeliveries()`，两段各自捕获异常，抓取失败不影响已入队邮件继续重试。到期判断仍由数据库 `next_run_at` 决定，心跳只负责唤醒，实际业务周期还是 30/60 分钟。

启动时先调用 `recoverInterruptedSiteMonitorRuns()`，把超过 15 分钟仍是 `running` 的运行记录标记为中断——该阈值远大于一次真实运行的耗时，不会误伤正在执行的运行，第三阶段遗留的“进程异常终止留下 running 记录”至此闭合。恢复完成后立即跑一次心跳，部署重启后已逾期的监测不必再等一个心跳周期。

### 7.2 管理员接口

`lcyksp-backend/src/routes/siteMonitor.js` 挂载在 `/api/admin/site-monitor`，路由内部先 `authMiddleware` 再 `requireAdmin`，因此鉴权随路由一起走。该前缀比 `/api/admin` 更具体，必须先挂载，否则每个请求会先在 adminRouter 里白做一次 JWT 校验和用户查询。

| 接口 | 作用 |
| --- | --- |
| `GET /` | SMTP 状态、心跳周期、两个来源的固定周期（只读）、开关、基线数量、最近检查/成功、下次检查、连续失败、错误摘要、最近 5 次运行、待发与失败邮件数 |
| `POST /:source/config` | 保存开关、认证方式、凭据、收件邮箱 |
| `POST /:source/diagnose` | 强制取完整正文做连接与解析诊断，不改基线与调度 |
| `POST /:source/check` | 立即检查一次，入队邮件时顺带执行一次投递 |
| `POST /:source/rebuild-baseline` | 清空快照并重建基线，不发送通知 |
| `GET /:source/events` | 事件分页，`pageSize` 上限 50，倒序 |
| `POST /test-email` | 管理员显式触发的真实测试邮件，15 秒冷却 |

凭据经 `encrypt()` 入库，响应只返回是否已配置、掩码和长度，任何接口都不回显明文；带换行的凭据、超长凭据、非法邮箱、以及“启用但没有凭据”都在写库前拒绝。请求体里的 `intervalSeconds` 一律忽略，周期由数据库约束固定。首次启用会把 `next_run_at` 置为当前时间，下一次心跳即生效。手动检查若入队了邮件会立刻执行一次投递，管理员当场就能看到发送结果，而不是等下一个心跳。

目标站点导致的失败一律返回 502，不返回 401——管理员本人是已登录的，用 401 表达“对方站点拒绝了我们的凭据”会同时误导前端与阅读日志的人；凭据被拒的情况改用响应里的 `credentialRejected` 标记，第六阶段可据此直接把光标指向凭据输入框。

### 7.3 基线重建

`resetSiteMonitorBaseline()` 在同一个事务里删除快照并清空 `ETag` 与 `Last-Modified`。不清验证器的话，重建后的第一次请求可能拿到 304 而没有正文，落到“没有基线却收到 304”的失败分支。重建使用 `baseline` 触发类型，只建基线不产生事件。

### 7.4 审计修正

接口测试发现测试邮件端点在收件人写错时会回退到数据库里的默认地址并照常发送，等于把管理员的输入错误吞掉。已改为：显式传入的地址非法直接返回 400，只有完全不传该字段时才使用监测配置里的收件人。

### 7.5 验证结果

新增 15 条测试（7 条调度、8 条接口），站点监测及既有后端回归统一执行，最终 62/62 通过；相关 JavaScript 文件通过 `node --check`。

调度覆盖：成功后按 30/60 分钟排下一次、未到期不发出任何请求、把 `next_run_at` 前推以模拟时间流逝后恰好执行一次、关闭的监测即使逾期两小时也不执行、失败退避从 5 分钟开始且不超过该来源的固定周期、成功后清零连续失败、超时的运行记录被标记而仍在有效执行窗口内的记录不受影响、基线重建清空快照与验证器且不产生事件。

接口覆盖：7 个端点在未登录返回 401、普通用户返回 403；概览响应不含凭据明文；凭据密文入库且响应只有掩码；固定周期无法通过接口改动；四类非法输入在写库前被拒绝；未知来源在触发抓取前返回 400；事件分页倒序且参数被收敛；测试邮件在 SMTP 未配置时返回 502、冷却返回 429、非法地址返回 400。

另做一次真实启动冒烟：用临时数据库启动后端，`/api/admin/site-monitor/` 无凭据返回 401，定时任务正常启动，日志没有异常与未处理拒绝。`diagnose`/`check`/`rebuild-baseline` 的成功路径会真的发起外网请求，接口测试只覆盖它们的鉴权与参数校验，抓取与差异逻辑由第三、四阶段的服务层测试覆盖。

### 7.6 已知行为

事件指纹与时间无关，因此同一个模型如果先下架、之后又重新上架，`model_added` 的指纹与首次相同，不会再发一次通知。这是第二阶段为保证重试幂等定下的口径。若需要“回归也要通知”，应在 `site_monitor_items` 上增加通知代次字段并纳入指纹，属于对第二、三阶段的改动，本阶段没有擅自修改。

## 8. 第六阶段：管理员界面的实现方式与核验

### 8.1 入口与信息结构

`lcyksp-front/src/views/AdminView.vue` 在“GitHub日报”与“会员配置”之间新增“网站监测”Tab，只在管理后台内，不给普通用户任何入口。页面按“先看状态、再动配置、最后查历史”排布：

- 顶部两枚标签给出 SMTP 是否配置与心跳周期，右侧是测试邮件与刷新；
- 两个来源各一张卡片：标题行是状态圆点、名称、固定周期标签、无基线提醒和启停开关；
- 卡片中部四个数字块（基线 / 变更 / 待发 / 连败）与两条时间（最近成功 / 下次检查），失败时下面追加一条红色错误摘要；
- 卡片下部是认证方式、凭据、收件邮箱三项配置和保存、诊断、立即检查、重建基线四个按钮，再往下是最近 5 次运行；
- 页面底部是变更记录：按来源切换，表格给出时间、类型标签和可点击的公告链接，超过一页才显示分页器。

状态用圆点和颜色表达而不是整句描述，固定周期、触发方式、事件类型都用标签，符合“砍文字、留视觉元素”的既有取向。周期只读展示，界面上没有任何可以改周期的控件。

### 8.2 凭据处理

凭据输入框是 password 类型，占位符显示后端返回的掩码与长度（如“已配置 ••••7788（32 字符），留空保持不变”）。保存成功后本地立即清空输入框，刷新时也只会写入空串，全程不进 localStorage，也没有任何接口会回显明文。留空保存表示保持原凭据不变。

### 8.3 时区与失败反馈

后端返回的是 SQLite 的 UTC 字符串（`YYYY-MM-DD HH:MM:SS`），直接交给 `new Date()` 会被当成本地时间、显示差 8 小时，因此前端统一补 `Z` 再转本地时间显示。上游失败沿用第五阶段的约定：接口返回 502 与 `credentialRejected`，界面把凭据过期的情况合并成一条提示（“……（凭据可能已过期，请更新后重试）”），不额外弹第二个提示。每张卡片有独立的忙碌状态，一个来源在执行时不会锁住另一个来源的按钮。

### 8.4 验证结果

前端 `npm run build` 通过。另外用临时数据库、种子数据和真实浏览器做了一次交互核验：

- Tab 出现在管理后台且顺序正确；两张卡片的统计、时间、错误摘要、最近运行、凭据掩码均按预期渲染；
- UTC→本地换算正确（种子里 4 分钟前、2 小时前的记录显示为对应本地时间）；
- 变更记录按来源切换正常，12 条记录分两页，公告链接指向惠州学院原文；总数不超过一页时分页器不出现；
- 保存配置、测试邮件均只发出一次请求（网络面板核对）；SMTP 未配置时界面提示“测试邮件发送失败：尚未配置 163 邮箱 SMTP 授权码”，不会白屏或静默；
- 点击“诊断”对惠州学院真实页面跑通了界面→接口→抓取→解析全链路，解析出 14 条公告，与第二阶段真实页面交叉验证的数量一致，且诊断没有改动基线数量、连续失败次数和调度时间；
- 暗色主题下卡片背景取到 `#16162a`（`--bg-card`），与站点其余部分一致；因为用的是主题变量而不是硬编码颜色，浅色主题同样正常；
- 控制台无本次改动引入的告警：核验时发现的 `el-pagination` 的 `small` 属性在 Element Plus 2.14 已弃用，已改为 `size="small"`，其余告警是 App.vue 既有的图标 `markRaw` 提示。

## 9. 第七阶段：跨层回归与可回滚部署的实现方式与核验

### 9.1 前六阶段交叉审计与修正

按“存储 → 抓取 → 解析 → 差异 → 队列 → SMTP → 调度 → 管理员接口 → 管理员界面”的调用链复核前六阶段。审计将监测事务移到独立 SQLite 连接，并开启外键与 10 秒 busy timeout，避免主业务语句误入监测事务；基线重建改为先抓取解析，再在事务中原子替换，网络或解析失败时保留旧快照、ETag 与 Last-Modified；SMTP 响应读取在成功、错误、超时和连接关闭路径统一移除监听器。

SSRF 防护同时在 URL 层固定 HTTPS 主机、在抓取前检查解析地址、在 `undici.Agent` 建连 lookup 阶段再次拒绝回环、私网、链路本地及保留地址，关闭 DNS 检查与实际建连之间的 rebinding 窗口。lookup 使用可注入 DNS 实现编写确定性测试：混合公网与 `127.0.0.1` 的结果返回 `EHOSTUNREACH`，纯公网结果保持地址和协议族。

### 9.2 自动化、真实站点与端到端核验

后端执行 `npm test`，62 个测试全部通过；其中邮件端到端用例覆盖“页面变化 → 事件入库 → 持久投递队列 → 模拟 SMTP 成功 → `sent_at` 写入”。17 个相关 JavaScript 文件通过 `node --check`，`git diff --check` 无空白错误。前端执行 `npm run build`，3847 个模块完成转换并生成压缩产物；Rollup 的 PURE 注释与大 chunk 提示属于既有构建警告，不阻断产物生成。

惠州学院真实 smoke 使用生产抓取器和生产解析器访问固定地址，得到 HTTP 200、`text/html` 和 14 条非空公告，首条为“惠州学院2026年硕士学位研究生招生目录”；该检查只在内存解析，不写正式数据库。JustWoker 真实链路使用管理员正常登录取得的 Bearer Token，请求固定的 `/api/pricing` JSON 接口；代码不尝试绕过上游认证。

### 9.3 发布包、数据库备份与回滚路线

`deploy.ps1` 只打包 `lcyksp-front/dist`、`lcyksp-backend/src`、`package.json`、`package-lock.json` 和 `ecosystem.config.cjs`，并拒绝 `.env`、`data`、`test` 及 `_*.mjs/png/py`，因此不会把仓库中的调试材料或生产数据库覆盖到服务器。 发布前复审修正了白名单正则仅接受目录本身、误拒绝目录内合法文件的问题；修正后重新生成的归档包含 348 个条目、大小约 58.71 MB，非法条目为 0，并明确包含前端 `index.html` 与后端监测路由。远端暂存目录先执行 `npm ci --omit=dev`，使锁文件中的 `sqlite3@5.1.7` 按服务器 Node 环境安装，依赖安装失败时线上进程不受影响。

数据库备份使用 sqlite3 online backup API 的 `backup → step(-1) → finish` 完整生命周期复制 WAL 一致性快照，随后对备份执行 `PRAGMA integrity_check`。 首次实际发布在远端备份校验脚本的语法编译阶段发现操作句柄与只读校验连接重名；脚本将校验连接改为独立变量名，失败发生在 PM2 停止与文件交换之前，线上应用未受影响。 再次预切换核验发现 stdin 模块从管理员家目录解析依赖，无法定位暂存目录的 `sqlite3`；备份命令因此改在暂存后端目录执行，两次失败均位于 `npm ci` 后、备份创建和停服之前。 第三次切换时应用文件与进程已进入验证阶段，生产库只读校验命令暴露同类模块解析路径问题并触发错误 trap；旧版 `src`、`dist` 与 `node_modules` 随即恢复，PM2、本机健康接口和公网健康接口复核正常。生产库校验命令改为在新后端目录执行后再发布。本地对同一封装执行 smoke，备份返回 `ok` 且数据行数一致。应用切换前保存 `src`、`dist`、`node_modules`、包清单和 PM2 配置；同文件系统移动缩短停服窗口。新版本健康检查、PM2 单 online 实例检查、生产库完整性检查或五张监测表检查失败时，错误 trap 恢复上一版应用文件并重启 PM2。数据库备份和回滚目录保留给人工追溯。

（9.10 起该白名单已收紧：`ecosystem.config.cjs` 不再随发布包上传，原因见 9.10。）

### 9.4 代码量统计口径

统计范围为 `lcyksp-front/src` 与 `lcyksp-backend/src` 下的源码，前端纳入 Vue、JavaScript/TypeScript、样式和 HTML，后端纳入 JavaScript/TypeScript；统一排除 `node_modules`、`dist`、`test`、覆盖率目录和以下划线开头的临时文件。按该口径：前端 60 个文件、28,912 行；后端 47 个文件、12,194 行；合计 107 个文件、41,106 行。行数包含空行和注释，便于后续用同一脚本复算。（这是第七阶段的快照；9.9–9.11 的新增代码未计入。）

### 9.5 生产切换与回滚材料

使用仅收紧本地 ACL 的临时 OpenSSH 私钥副本连接 `admin@<生产服务器>`，服务器主机指纹继续由 OpenSSH 已知主机记录校验。发布脚本完成服务器预检、白名单包上传、暂存区生产依赖安装、SQLite 在线备份、应用文件交换、PM2 重启和数据库复核。生产发布编号为 `20260910-103851-593761`；一致性备份位于 `<后端目录>/data/db/database.db.before-site-monitor-20260910-103851-593761.bak`，大小 14,090,240 字节；应用回滚目录为 `<部署基目录>/.deploy-rollback-20260910-103851-593761`，保存 `src`、`dist`、`node_modules`、包清单、PM2 配置和发布记录。

生产复核确认 PM2 中只有一个名为 `lcyksp-backend` 的 online 实例，本机及公网 `/api/health` 均返回 `status=ok`；生产 SQLite 的 `PRAGMA integrity_check` 返回 `ok`，且五张 `site_monitor_%` 表齐全。部署后的静态目录继承暂存目录 `umask 077`，首次公网根路径核验因此返回 403；检查 Nginx 错误边界和目录权限后，将生产 `dist` 修正为目录 755、文件 644，公网首页和管理后台资源均返回 200。发布脚本同步加入 `chmod -R u=rwX,go=rX "$BACKEND/dist"` 和公网首页内容检查，后续发布会在脚本内阻断同类静态权限回归，而不只检查 API 健康。

### 9.6 生产功能验收

生产数据库中的两项监测周期分别为 1800 秒和 3600 秒，收件地址均为 `1296757861@qq.com`，验收期间开关均保持关闭。SMTP 配置探测返回可用，并真实发送一封“网站监测测试邮件”到指定邮箱，SMTP 服务器返回 250。惠州学院使用已发布抓取器和解析器执行诊断，HTTP 200 并解析 14 条公告；随后原子建立 14 条基线，未生成历史事件或邮件批次。JustWoker 首次生产诊断仍指向 `/pricing` SPA 文档，因此即使保存 Bearer Token也会得到 HTTP 200 的 HTML 并触发内容类型保护。浏览器 XHR 与服务器端只读探测随后交叉确认真实接口为 `https://api.justwoker.icu/api/pricing`：当时携带数据库中加密保存且尚未过期的 Bearer Token 后返回 HTTP 200、`application/json`，响应包含 3 个 `model_name` 项；随后生产验收出现 401，进一步确认该 Token 会过期，不能承担长期轮询。修订版将默认地址及旧地址定向迁移到 `/api/pricing`，清除旧验证器和旧错误状态，并让解析器显式支持该真实响应结构；基线仍需在修订版生产诊断通过后建立。

生产日志从本次发布启动点起没有站点监测启动错误，凭据模式扫描没有发现 `Authorization`、Bearer、Cookie、`auth_secret` 或私钥内容。部署产物中的 `AdminView` 资源包含“网站监测”Tab，公网首页、该资源和健康接口均返回 200。生产依赖安装仍报告 13 项审计风险（2 low、3 moderate、7 high、1 critical）；未使用破坏兼容性的强制升级，本项需要另行做依赖升级兼容评审。

### 9.7 JustWoker 真实接口修订

浏览器开发者工具显示模型广场页面通过 `GET https://api.justwoker.icu/api/pricing` 获取数据，请求使用 `Authorization: Bearer <token>`；服务器端使用数据库内加密凭据执行不输出密钥的只读结构探测，得到 HTTP 200、JSON 正文和 3 个模型项。原配置访问 `/pricing` 时拿到的是 React 单页应用 HTML，因此错误表现为“unexpected content type”，与 Token 是否有效无关。修订将抓取地址改为 `/api/pricing`，启动时仅把旧内置地址定向迁移到新地址，保留认证配置、开关和基线数据，同时清除与旧 URL 绑定的 ETag、Last-Modified、失败计数及调度时间；解析器把 `model_name` 纳入稳定标识字段。回归测试加入真实响应形状和最终请求 URL 断言，避免页面地址与 API 地址再次混淆。

### 9.8 JustWoker 长期认证修订与交叉核验

生产修订版用现有的 `cookie` 类型表达 JustWoker 刷新会话，以避免重建 SQLite 约束表。抓取器遇到“JustWoker + Cookie”时，先对同一白名单主机发起 `POST /api/user/auth/refresh`，只发送 Cookie，不发送旧 Bearer；仅接受无重定向的成功 JSON，并严格检查 `success === true`、`token_type` 为 Bearer、`access_token` 非空且长度与换行符合限制。刷新成功后，模型请求只发送新 Bearer，不携带会话 Cookie。其他来源的 Cookie 仍按原方式直接发送，临时 Bearer 路径也保持兼容。

短期 access token 不包含在抓取返回值、错误、运行记录或数据库中。刷新响应若设置新 Cookie，代码只提取 Cookie 名值对、合并轮换内容并在模型请求成功后重新加密写入 `auth_secret`；Path、HttpOnly、Secure 等属性不会被误发为 Cookie。超时信号、DNS 私网阻断、HTTPS 主机白名单、手动重定向检查和响应体积上限同时覆盖刷新请求与模型请求。

回归用例分别验证了刷新请求方法与请求头、短期 Token 的后续 Bearer 使用、401/403、非 JSON、缺失 Token、错误 Token 类型、Cookie 轮换与删除、加密持久化、敏感值不进入结果和运行记录、惠州学院 Cookie 行为不变以及旧 Bearer 兼容行为。后端完整测试共 69 项，前端构建用于核验管理员界面中的“登录会话 Cookie（推荐）”和“临时 Bearer Token（仅诊断）”选项。生产 401 的结论保留在报告中，避免再把一次性复制的 Bearer 误认为长期凭据。

修订版发布编号为 `20260910-123002-596079`，数据库在线备份位于 `<后端目录>/data/db/database.db.before-site-monitor-20260910-123002-596079.bak`，应用回滚目录为 `<部署基目录>/.deploy-rollback-20260910-123002-596079`。发布脚本完成构建、生产依赖安装、原子交换、PM2 与内外网健康检查；复核确认单个 PM2 实例 online、SQLite 完整性为 ok、公网首页与健康接口返回 200/ok。惠州学院保持开启、基线健康；JustWoker 保持关闭且无基线，数据库中的旧 Bearer 不会自动转换为会话 Cookie，管理员需按新界面说明保存有效 Cookie 后再诊断。

### 9.9 监测健康告警与 JustWoker 登录会话的长期可用性

JustWoker 的模型接口必须登录才能读，而登录会话最终会失效。失效时监测持续返回 401：旧基线和已发现的事件都还在，但**新模型不会再有通知**，界面上只体现为“连败”数字递增——这类静默停摆正是本节要解决的问题。凭据本身的持久化在 9.8 已经完成（Cookie 加密入库、每轮先刷新短期 Token、上游轮换的 Cookie 重新加密回写）；本节补上“失效必须有人知道”。

**事件与邮件**：`site_monitor_events` 的 `monitor_failed` / `monitor_recovered` 两种类型在第一阶段就写进了 CHECK 约束，管理员界面的标签（`AdminView.vue:161-162`）也早已存在，但此前没有任何代码写入它们。本节在 `siteMonitorService.js` 中接上：失败时写 `monitor_failed` 并投递一封“监测中断”邮件，恢复时写 `monitor_recovered` 并投递一封“监测已恢复”邮件。邮件复用 `site_monitor_deliveries` 表，因此重试、锁定、去重、退避全部沿用第四阶段的既有实现，没有第二套投递通道。

**告警口径**（本节的核心，改动前必须先读）：

| 情况 | 是否发信 | 理由 |
| --- | --- | --- |
| 凭据被拒（401/403，或 AUTH_REJECTED / AUTH_MISSING / AUTH_INVALID） | 立即发，一场故障一封 | 只有管理员能处理，且这正是 JustWoker 会话过期的表现 |
| 其他失败连续达到 3 次 | 发一封 | 单次网络抖动不值得打扰，持续失败才需要人看 |
| 其他失败不足 3 次 | 不发 | 避免噪声 |
| 已就同一场故障发过信，故障继续 | 不再发 | 30 分钟一轮的轮询若每轮都发信，一天会有 48 封 |
| 非凭据故障的告警仍敞开时又出现凭据被拒 | **追加发一封** | 凭据失效必须升级；`payload_json` 记了 `credentialRejected`，用来区分“已经按凭据告过”和“只是别的故障” |
| 恢复 | 只在之前确实发过告警时发一封 | 没告过警就不必报喜 |
| 诊断（diagnose）失败 | 不发 | 诊断不改监测健康状态，管理员当场就能在界面上看到错误 |

**一场故障的判定**不看失败次数，而看 `site_monitor_events` 里最新一条健康事件是不是 `monitor_failed`：是就说明这场故障已经告过警，否则就是新的一场。因此“故障 → 恢复 → 再故障”的反复不会重复轰炸，而真正的新故障仍会告警。

**幂等**：告警事件的 `item_key` 用当次 run id（`alert:<runId>` / `recovery:<runId>`），事件指纹唯一；投递去重键由该事件指纹派生，与变更通知共用同一套 `ON CONFLICT(dedupe_key) DO NOTHING`。事件写入与投递入队和运行结果、健康状态在同一个事务里提交，不会出现“邮件队列里有、数据库里没有”的悬空告警。

**时间**：`last_success_at` 与 `datetime('now')` 都是 SQLite 的 UTC 字符串，邮件里直接打印会比北京时间早 8 小时。告警邮件统一用新增的 `beijingTime()` 换算并标注“（北京时间）”，与管理员界面补 `Z` 的处理口径一致。

**密钥卫生**：告警正文的错误摘要先经 `publicError()` 把 `authorization/cookie/bearer/token` 后面的值替换成 `[redacted]`；测试断言告警邮件正文既不含 Cookie 明文也不含 Token 明文。

**即时投递**：`POST /:source/check` 在入队了变更通知**或**健康告警时都会立刻投递一次，所以管理员重贴 Cookie 后点“立即检查”，恢复邮件当场发出。定时轮询产生的告警由五分钟心跳投递，最迟 5 分钟到达。失败路径的告警不在响应前发送（管理员已在界面上看到 502），交给下一次心跳。

**验证**：新增 `test/siteMonitorAlerts.test.js` 8 条用例，覆盖凭据被拒只告警一次、Cookie 刷新被拒时不再请求模型接口、恢复只报一次且之后的新故障会再次告警、非凭据失败不足 3 次保持静默、诊断失败不告警、非凭据告警期间凭据失效会升级、正文转义与主题换行清除、`beijingTime()` 边界。后端全量 **77/77 通过**，改动文件通过 `node --check`。管理员界面无需改动：这两类事件没有 URL，事件表按纯文本渲染，标签本来就已存在。

### 9.10 健康告警的发布与 ecosystem.config.cjs 风险处置

发布编号 `20260910-184533-597554`，数据库一致性备份 `<后端目录>/data/db/database.db.before-site-monitor-20260910-184533-597554.bak`，应用回滚目录 `<部署基目录>/.deploy-rollback-20260910-184533-597554`。发布后复核：PM2 单个 `lcyksp-backend` 实例 online、本机 `/api/health` 与公网 `/api/health` 均为 `status=ok`、公网首页 200、五张 `site_monitor_%` 表齐全，部署产物中 `recordFailureAlert`、`queueSiteMonitorAlertDelivery`、`beijingTime`、`result.alert?.queued` 均已存在。

**本机没有 PuTTY**，而 `deploy.ps1` 在给定 `SERVER_PASSWORD` 时强制要求 `plink`/`pscp`，因此本次改用 `tools/deploy-full.py`（paramiko，与 `tools/deploy-front.py` 同一套连接约定）上传并执行 **`deploy.ps1` 内嵌的同一份远程脚本**；打包与白名单校验仍在 PowerShell 侧按原命令完成，远程逻辑没有第二份实现。

**发现并修正了一处会打死后端的缺陷**：`deploy.ps1` 原来把 `./lcyksp-backend/ecosystem.config.cjs` 打进发布包，内嵌远程脚本再用 `cp -p` 覆盖服务器上的同名文件。而仓库里那份是 stub——`JWT_SECRET` 被注释掉，真正的密钥由 `tools/configure-server.py` 直接注入服务器文件。`src/middleware/auth.js:10-12` 在 `NODE_ENV=production` 且缺 `JWT_SECRET` 时 throw 拒绝启动，后端里也没有任何 dotenv 兜底，所以覆盖它等于把后端打死——这正是第 10 节第一条明令禁止的事，脚本本身却一直在做。**该缺陷已经落地过一次**：本次核查发现服务器上的 `ecosystem.config.cjs` 就是那份 39 行 stub（未注释的 `JWT_SECRET:` 赋值 0 处、含 `REPLACE_WITH_RANDOM_64_HEX` 占位符、`ALS_API_KEY:` 0 处）。后端目前仍能启动，唯一原因是脚本用 `pm2 restart lcyksp-backend`，而 `pm2 restart` 复用 PM2 存留的环境变量、不重读 ecosystem 文件；`pm2 jlist` 确认在线进程仍带 `NODE_ENV=production`、`JWT_SECRET`、`ALS_API_KEY`。

修正内容：`deploy.ps1` 打包清单去掉该文件（白名单同步收紧，它一旦重新出现在包里就会被判为非法条目并中止）；内嵌远程脚本的 `cp -p` 不再包含它，但**回滚备份仍保留它**，所以换版失败时它会照旧被恢复；预检新增一行诊断，且匹配的是**未注释的赋值行** `^[[:space:]]*JWT_SECRET:`——最初写成 `grep -q 'JWT_SECRET'`，会被 stub 注释里的同名字样骗成“已注入”，属于假阳性，已修掉。

**遗留项已处置**：`tools/restore-ecosystem.py` 从**在线进程的实际环境**取值写回 `ecosystem.config.cjs`（不改密钥，已登录用户不受影响），验证文件可 `require`、`NODE_ENV=production` 且变量齐全后 `pm2 startOrReload ecosystem.config.cjs --update-env`，健康检查失败即回滚到备份并 `pm2 restart`。执行结果：文件由 39 行变为 44 行、未注释的 `JWT_SECRET:`/`ALS_API_KEY:` 各 1 处、权限 600，在线实例仍为 1 个 online，本机与公网健康检查均 ok；备份留在 `ecosystem.config.cjs.before-restore-20260910105048.bak`。随后 18:52 那次发布（`20260910-185217-598759`）复核确认该文件**没有再被覆盖**（仍 44 行、活动赋值各 1 处），即上面的修正确实生效。

**这一步差点踩坑**：写回前先枚举了在线进程实际的环境变量名，发现除了 stub 里那 4 个，还有 `DOUYIN_DEBUG`、`GITHUB_PROXY_URL`、`GITHUB_REQUEST_TIMEOUT_MS` 三个在用的配置（分别给 `video.js` 的调试转储和 `githubRadar.js`/`githubAi.js` 的 GitHub 代理与超时）。若只照 stub 写回那 4 个再 `--update-env`，这三个会被丢掉，GitHub 日报的代理与超时设置随之失效。教训：**恢复 PM2 环境必须按在线实际变量整组搬，不能照着 stub 抄。**

### 9.11 生产上 JustWoker 四次 401 的根因：凭据粘的是“值”而不是“请求头”

发布后只读核查生产库，`site_monitor_runs` 里 JustWoker 的记录是 6 条，全部是 `diagnose`：id 7（03:28Z，HTTP 200，`unexpected content type`，即迁移前的 `/pricing` SPA 地址）、id 9（03:54Z，401）、id 11–14（04:55Z，连续四次 401）。04:55Z 就是北京时间 12:55，正好在 9.8 那次发布（12:30 北京时间）之后 25 分钟，也就是管理员在新界面里第一次尝试“登录会话 Cookie”。

把库里那条凭据解密后**只统计形状、不输出内容**，得到：`auth_type = cookie`、长度 101、`cookieNames` 只有一项且等于整串——即整条凭据是一个 `<uuid>.<64 位十六进制>` 形状的**裸值**（此处不记具体值），整串里没有任何 `=`。这说明管理员是从浏览器开发者工具里复制了**单个 Cookie 的值**，而不是 `Cookie` 请求头（请求头必然是 `name=value` 列表）。后端于是发出 `Cookie: <裸值>`，new-api 的 `c.Cookie("session")` 取不到会话，直接回 `AUTH_UNAUTHORIZED`。

所以那四次 401 是**粘贴格式问题，不是会话过期**；同时也说明“Cookie 刷新”这条链路至今**没有在真实上游上用正确格式验证过**（9.8 的用例全是脱敏夹具），9.6/9.8 记录的 401 与这四次是两回事。

**据此加了一道输入校验**：`POST /:source/config` 在 `authType = cookie` 且本次提交了新凭据时，要求凭据里至少有一组 `name=value`，否则返回 400 并明确提示“请复制完整的 Cookie 请求头，而不是单个 Cookie 的值”。bearer 类型不受影响；留空保存（沿用原凭据）也不受影响。`test/siteMonitorAdminApi.test.js` 增加一条裸值用例（样例值一律用合成数据，绝不把真实凭据写进仓库），后端全量 **77/77 通过**。该校验已随 `20260910-185217-598759` 发布上线（部署产物中 `name=value` 出现在 2 处）。

## 10. 部署及启用注意事项

1. 发布脚本在暂存目录安装生产依赖、执行 SQLite 在线备份、交换应用文件、修复静态资源读取权限，再验证 PM2、数据库、本机 API、公网 API 与公网首页；任一切换后检查失败都会恢复上一版应用文件。发布包**不含** `ecosystem.config.cjs`：服务器那份承载注入的 `JWT_SECRET` 与 `ALS_API_KEY`，脚本不再覆盖它（背景与现状见 9.10）。
2. 惠州学院已经具备生产基线，可在管理员确认测试邮件到达后打开开关；打开后由五分钟心跳触发到期查询，业务间隔仍固定为一小时。
3. JustWoker 使用 `/api/pricing` 与登录刷新会话 Cookie。管理员在已登录的浏览器开发者工具中，从 `/api/user/auth/refresh` 请求复制 Cookie 请求头并选择“登录会话 Cookie（推荐）”。**必须复制整条请求头的值（形如 `session=xxxx; 其它名=值`），不能只复制 Cookies 表格里单个 Cookie 的值**——裸值没有 `name=`，请求头不合法，接口会以 400 直接拒绝（见 9.11）；这正是 9 月 10 日那四次 401 的原因。服务端每次轮询先刷新短期 Token，Token 只存在于当次请求内存，不写入数据库、日志、仓库、PM2 配置或前端 localStorage。若上游轮换 Cookie，后端仅将合并后的 Cookie 重新加密保存。临时 Bearer 只用于诊断，不作为定时方案。配置后执行“诊断 → 重建基线 → 再诊断”，确认模型数稳定后再由管理员手动开启。会话失效时后端会自动发一封告警邮件（见 9.9），管理员据此重新粘贴 Cookie，恢复后还会收到一封恢复邮件；这是该方法能长期无人值守运行的前提。
4. 目标网站结构变化时，解析保护会让任务失败并保留旧基线，不产生“全部下架”类错误告警；邮件投递失败会留在持久队列按退避策略重试。
5. 测试邮件已经由服务器提交到 QQ 邮箱的 SMTP 接收链路；最终收件箱到达情况仍需管理员在 QQ 邮箱中确认，必要时同时检查垃圾邮件目录。
